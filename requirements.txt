# Core runtime dependencies for EVEE.
# Reference environment: conda env "Zj" on Python 3.10.11.
#
# Install PyTorch separately before this file, matching your CUDA version.
# The tested environment uses:
#   torch==2.1.2+cu121
#   torchvision==0.16.2
#
# Example:
#   pip install pip==24.0 setuptools==69.5.1 wheel==0.43.0
#   pip install torch==2.1.2 torchvision==0.16.2 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#   pip install --no-build-isolation -r requirements-mamba.txt

setuptools==69.5.1
wheel==0.43.0
numpy==1.26.4
opencv-python==4.10.0.84
pillow==10.3.0
packaging
ninja

# Optional acceleration. Install only if it matches your CUDA setup.
# cupy-cuda12x==13.3.0
